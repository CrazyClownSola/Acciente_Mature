<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.dome.appstore"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.activity.RxWelcomeActivity_"
            android:screenOrientation="portrait"
            android:theme="@style/MaterialFullScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.RxMainActivity_"
            android:screenOrientation="portrait"
            android:theme="@style/MaterialTheme"
            android:windowSoftInputMode="stateHidden"
            />

        <activity
            android:name=".ui.activity.DetailActivity_"
            android:screenOrientation="portrait"
            android:theme="@style/MaterialTheme"
            />

        <activity
            android:name=".ui.activity.DemoActivity_"
            android:theme="@style/MaterialTheme"
            />

    </application>

</manifest>